<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gzu.ecommerce.mapper.ProductMapper">

    <select id="findAll" resultType="com.gzu.ecommerce.entity.Product">
        SELECT id, name, description, price, stock, category_id AS categoryId, image_url AS imageUrl
        FROM product
        ORDER BY id DESC
    </select>

    <select id="search" resultType="com.gzu.ecommerce.entity.Product">
        SELECT id, name, description, price, stock, category_id AS categoryId, image_url AS imageUrl
        FROM product
        WHERE 1=1
        <if test="name != null and name != ''">
            AND name LIKE CONCAT('%', #{name}, '%')
        </if>
        <if test="categoryId != null">
            AND category_id = #{categoryId}
        </if>
        ORDER BY id DESC
    </select>

    <select id="findById" resultType="com.gzu.ecommerce.entity.Product">
        SELECT id, name, description, price, stock, category_id AS categoryId, image_url AS imageUrl
        FROM product
        WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product (name, description, price, stock, category_id, image_url)
        VALUES (#{name}, #{description}, #{price}, #{stock}, #{categoryId}, #{imageUrl})
    </insert>

    <update id="update">
        UPDATE product
        SET name = #{name},
            description = #{description},
            price = #{price},
            stock = #{stock},
            category_id = #{categoryId},
            image_url = #{imageUrl}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM product WHERE id = #{id}
    </delete>

    <update id="decreaseStock">
        UPDATE product
        SET stock = stock - #{quantity}
        WHERE id = #{id} AND stock >= #{quantity}
    </update>

</mapper>